name = "10x-flashcard-builder"
compatibility_date = "2024-11-21"
compatibility_flags = ["nodejs_compat"]
pages_build_output_dir = "./dist"

# KV Namespace for sessions
# This binds the KV namespace to the SESSION variable in the code
[[kv_namespaces]]
binding = "SESSION"
id = "d7182b523f2c437c872e88b271e8b68f"
# Environment variables are configured in Cloudflare Dashboard and GitHub Secrets
# They are injected during build time and available via import.meta.env
# nodejs_compat flag enables Node.js compatibility features including:
# - process.env support (auto-populated from Cloudflare env vars)
# - Basic Node.js APIs (crypto, buffer, util)
# - Better npm package compatibility
